sig g4ip.
accum_sig mimic.
type infer4	option index -> list index -> list index -> cert.
type gip 	int -> list index -> cert.
type oneStep 	index -> cert.

type dlA index -> index.
type $l, $r, $1,$2 index. 

type changeIndex list (pair index index) -> o.
% Utils

type insertDelays form -> form -> o.
